apiVersion: v1
kind: Service
metadata:
  name: openldap
spec:
  type: ClusterIP
  selector:
    app: openldap
  ports:
    - name: http
      port: 389
      targetPort: 389
    - name: https
      port: 636
      targetPort: 636