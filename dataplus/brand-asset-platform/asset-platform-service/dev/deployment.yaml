apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: asset-platform-service
  name: asset-platform-service
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: asset-platform-service
  template:
    metadata:
      labels:
        app: asset-platform-service
    spec:
      containers:
      - image: 10.101.2.123/library/asset-platform-service:{{ .tag }}
        imagePullPolicy: IfNotPresent
        name: asset-platform-service
        ports:
        - containerPort: 9530