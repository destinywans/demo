apiVersion: v1
kind: Service
metadata:
  name: basic-platform
  namespace: dev
  labels:
    app: basic-platform
spec:
  type: ClusterIP
  selector:
    app: basic-platform
  ports:
    - name: http
      port: 8080
      targetPort: 8080